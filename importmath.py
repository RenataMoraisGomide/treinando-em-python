import math
num = int(input('Digite um número: '))
raiz = math.sqrt(num)
print('A raiz de {} é igual a {}' .format(num, math.floor(raiz)))

#Nesse exercício usamos a import (biblioteca) de math(matemática) para
#calcular a raiz quadrada (sqrt) de um número qualquer, usamos o math.ceil (matemática.teto) para
#arredondar o resultado para cima ou math.floor (matemática.chão) arredondando para baixo.
#import randon o computador escolhe o número ex:


nume = float(input('Digite um número: '))
num = nume
print('A parte inteira desse número é: ' .format(num, math.ceil(nume)))




import random
num = random.randint(1, 10)
print(num)



